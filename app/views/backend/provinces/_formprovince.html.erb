<div class="box box-danger">
  <div class="box-header with-borde">
    <%= simple_form_for @province do |p| %>
      <%= p.input :name %>

      <%= p.button :submit, class: 'btn btn-success btn-flat'%>
    <% end %>
  </div>
</div>
